---
import { Image } from "astro:assets";
import { markdownify } from "@/lib/utils/textConverter";

const { title, content, image, button } = Astro.props;
const images = import.meta.glob<{ default: ImageMetadata }>(
  "/src/assets/**/*.{jpeg,jpg,png,gif}",
  { eager: true }
);

const imageSrc = images[image]?.default ?? image;
---

<section class="section px-4">
  <div class="section container rounded-xl shadow">
    <div class="row mx-auto items-center justify-center">
      <div class="md:col-5 lg:col-4">
        <Image
          class="w-full"
          src={imageSrc}
          alt="call to action image"
          width={325}
          height={206}
        />
      </div>
      <div class="mt-5 text-center md:mt-0 md:text-left md:col-6 lg:col-5">
        <h2>{title}</h2>
        <p class="mt-6" set:html={markdownify(content)} />
        {
          button.enable && (
            <a class="btn btn-primary mt-4" href={button.link} rel={button.rel}>
              {button.label}
            </a>
          )
        }
      </div>
    </div>
  </div>
</section>
